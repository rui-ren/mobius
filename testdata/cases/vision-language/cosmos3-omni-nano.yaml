model_id: "nvidia/Cosmos3-Nano"
model_type: "cosmos3_omni"
revision: "411f42a8fdfb8c5b2583cb8786e0938f49796eaa"
task_type: "image-text-to-text"
dtype: "float32"

inputs:
  prompts:
    - "Describe this image in detail."
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

notes: >
  This VLM case validates the Cosmos3-Omni understanding tower ("Reasoner")
  independently. Architecturally it is Qwen3-VL (interleaved 3D M-RoPE,
  QK-norm, DeepStack layers [8, 16, 24]). Complete world-model export,
  including Generator, Wan VAE, Sound, and Action, uses
  build_world_model() / --features world-model.
