model_id: "vibevoice/VibeVoice-1.5B-hf"
model_type: "vibevoice"
revision: "edc39f80f5cae656da37baf8faa8f5502bf7081f"
task_type: "text-to-speech"
dtype: "float16"
level: "L4+L5"

inputs:
  prompts:
    - "Mobius verifies text-only speech."

generation:
  max_new_tokens: 3
  do_sample: false
  exact_match: true
  num_diffusion_steps: 2
  guidance_scale: 1.3

notes: >
  Text-only VibeVoice processor and generation contract. This case intentionally
  supplies no waveform and therefore must use the embedding graph's zero-row
  optional-audio input instead of synthesizing a fake reference.
